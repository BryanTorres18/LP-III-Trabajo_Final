import javax.swing.*;

public class Selector_nivel extends JFrame {
    private JPanel panel1;
    private JButton button1;

    public Selector_nivel() {
        setTitle("Agenda");
        setContentPane(panel1);
        setSize(800, 600);
        setVisible(true);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
}
